[[ -z $op_file ]] && op_file="*"
[[ -z $session ]] && session=compiler

case $session in
    lib)
        dir=(lib lib_os/unix)
        find_opts=(-name ${op_file:l}.li)
        ;;
    compiler)
        dir=(src)
        find_opts=(-name ${op_file:l}.li)
        ;;
esac

# kate: hl Bash;
